# Repository workflow

- Work directly on `main` unless the user explicitly requests a branch or pull request.
- For approved source or documentation changes: edit, verify, commit, and push `main`.
- Do not create or use pull requests, draft-review transitions, or merge commits unless the user explicitly requests them.
- Do not publish NuGet packages or modify consumer repositories without separate explicit approval.
